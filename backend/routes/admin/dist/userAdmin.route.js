"use strict";
exports.__esModule = true;
var express_1 = require("express");
var userAdminRoute = express_1["default"].Router();
var userMethods = require('../../controllers/user.controller');
var ensureAdmin = require('../../config/ensureRoles').ensureAdmin;
var userRouter = require('../general/user.route');
userAdminRoute.get('/', ensureAdmin, userMethods.findAllUsers);
userAdminRoute.use('/', ensureAdmin, userRouter);
exports["default"] = userAdminRoute;
